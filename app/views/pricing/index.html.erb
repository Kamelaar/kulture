<section class="section">

<p class = "subtitle has-text-centered">Afin de bénéfécier de notre service, vous devez souscrire à une abonnement. Vous aurez ainsi droit à un téléchargement illimité

  <div class="has-text-centered box">
    <h1 class="title">Abonnements</h1>
    <h2 class="subtitle">Choisissez l'abonnement qui vous correspond.</h2>
    <div class="pricing-table">
      <div class="pricing-plan is-warning">
        <div class="plan-header">Trimestriel</div>
        <div class="plan-price"><span class="plan-price-amount"><span class="plan-price-currency">€</span>19.99</span>/trimestre</div>
        <div class="plan-items">
          <div class="plan-item">Téléchargement illimité</div>
        </div>
        <div class="plan-footer">
          <% if user_signed_in? %>
            <%= link_to "S'abonner", new_subscription_path(plan: "trimestriel", plan_id: Rails.application.credentials.trimestriel), class:"button is-fullwidth" %>
          <% else %>
            <%= link_to "S'abonner", new_user_registration_path, class: 'button is-fullwidth' %>
         <% end %>
        </div>
      </div>
      <div class="pricing-plan is-link">
        <div class="plan-header">Semestriel</div>
        <div class="plan-price"><span class="plan-price-amount"><span class="plan-price-currency">€</span>29.99</span>/semestre</div>
        <div class="plan-items">
          <div class="plan-item">Téléchargement illimité</div>
        </div>
        <div class="plan-footer">
           <% if user_signed_in? %>
            <%= link_to "S'abonner", new_subscription_path(plan: "semestriel", plan_id: Rails.application.credentials.semestriel), class:"button is-fullwidth" %>
          <% else %>
            <%= link_to "S'abonner", new_user_registration_path, class: 'button is-fullwidth' %>
         <% end %>
        </div>
      </div>
      <div class="pricing-plan is-danger">
        <div class="plan-header">Annuel</div>
        <div class="plan-price"><span class="plan-price-amount"><span class="plan-price-currency">€</span>39.99</span>/année</div>
        <div class="plan-items">
          <div class="plan-item">Téléchargement illimité</div>
        </div>
        <div class="plan-footer">
         <% if user_signed_in? %>
            <%= link_to "S'abonner", new_subscription_path(plan: "annuel", plan_id: Rails.application.credentials.annuel), class:"button is-fullwidth" %>
          <% else %>
            <%= link_to "S'abonner", new_user_registration_path, class: 'button is-fullwidth' %>
         <% end %>
        </div>
      </div>
    </div>
  </div>
</section>